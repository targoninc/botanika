export class BotanikaFeature {
    static name = 'botanika';

    static isEnabled() {
        return true;
    }

    static enable(app, context, db) {}

    static getEndpoints() {
        return [];
    }

    static getIntents() {
        return [];
    }
}